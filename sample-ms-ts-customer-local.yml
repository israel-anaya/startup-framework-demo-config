server:  
  port: ${PORT:0}
  tomcat:
    threads:
      max: 5
spring:  
  application:
    name: sample-ms-ts-customer
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
logging:
  level:    
    root: info
