server:
  port: ${PORT:8081}
spring:
  application:
    name: startup-msa-admin-server-${spring.profiles.active}
logging:
  level:
    org.springframework.http.server.reactive: INFO
    org.springframework.web.reactive: INFO
    org.springframework.boot.autoconfigure.web: INFO
