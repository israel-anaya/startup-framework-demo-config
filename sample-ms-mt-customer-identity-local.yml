server:  
  port: ${PORT:0}
  tomcat:
    threads:
      max: 5
spring:  
  application:
    name: sample-ms-mt-customer-identity
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
logging:
  level:    
    root: info
